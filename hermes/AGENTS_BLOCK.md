<!-- ORACULO:START — optional block for your project's AGENTS.md -->
## ORÁCULO is active in this project

Repetitive tasks here graduate into compiled .py scripts. Before solving any
routine-looking task with the LLM, run `oraculo status`: tasks marked
`promovida` MUST be executed via `oraculo run <name> -- <command...>` (0
tokens, milliseconds, identical output). Route new repetitive commands through
the same wrapper so they can graduate too. Details: `~/.hermes/skills/oraculo/`.
<!-- ORACULO:END -->
